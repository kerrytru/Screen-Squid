Notes:
1. $ sudo tar xzf screensquid_v1_11.tar.gz -С /var/www/
2. $ sudo mv /var/www/html /var/www/screensquid/
3. $ mysql -u[user_name] -p[password]
4. mysql> CREATE DATABASE squidreport DEFAULT CHARACTER SET=utf8;
5. mysql> SOURCE /var/www/squidreport/createdb/createdb.sql
6. mysql> CREATE USER 'user_name'@'localhost' IDENTIFIED BY 'password';
          GRANT ALL PRIVILEGES ON squidreport.* TO 'user_name'@'localhost';

7. $ sudo vi /var/www/squidreport/config.php
  
     $db = "squidreport";
     $user = "user_name";
     $pass ="password";
     $address ="localhost";
     $cfgsquidhost[0]="localhost";
     $cfgsquidport[0] = 3128;
     $cfgcachemgr_passwd[0]="";

8. sudo vi /var/www/squidreport/fetch.pl
  
     my $user = "user_name";
     my $pass = "password";
     my $db = "squidreport";
   
     #set where access.log is
     my $filetoparse="/var/log/squid/access.log";

After installation you may also wish to:

1. sudo touch /var/www/squidreport/fetch.sh
2. sudo vi /var/www/squidreport.fetch.sh

   #!/sbin/sh
   cd /var/www/squidreport/
   perl /var/www/squidreport/fetch.pl

3. sudo crontab -e
4. 00 23 * * * /var/www/squidreport/fetch.sh
